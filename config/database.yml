# SQLite version 3.x
# gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  host: localhost
  database: biotools_development
  username: root
  password:
 
# Warning: The database defined as 'test' will be erased and
# re-generated from your development database when you run 'rake'.
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    host: localhost
    database: biotools_test
    username: root
    password:
 
production:
    adapter: mysql
    host: localhost
    database: biotools_production
    username: root
    password: redsun109
    socket: /var/run/mysqld/mysqld.sock
